# Copy this file to .env and fill in real values.
# NEVER commit the real .env — see .gitignore.

# Google OAuth web client id used for the in-app Google Sign-In flow.
# This is a public identifier (not secret) but keep it in .env for easy swaps
# between dev/staging/prod OAuth clients.
VITE_GOOGLE_CLIENT_ID=
